<!-- Monkstone, 2012-November-02 -->
<COMMANDO>
<UI>
<CAPTION LABEL="Run">
<FILE_ENTRY LABEL="python file" VARNAME="file" EVAL="buffer.getName()"/>
</CAPTION>
<CAPTION LABEL="Path to PYP5">
<ENTRY LABEL="(no spaces)" VARNAME="PYP5" DEFAULT="/home/tux/bin/"/>
</CAPTION>
</UI>

<COMMANDS>
<!-- NB: I found that if I cd to the directory of the sketch, the data folder is not recognized, 
     So now I call the file at its absolute address using the prefix dir, and it works
-->
<COMMAND SHELL="System" CONFIRM="FALSE">
      buf = new StringBuilder(100);
      dir = MiscUtilities.getParentOfPath(buffer.getPath());
      dir = dir.replace(" ", "\\ ");
	  buf.append(PYP5).append("pyp5 ");
	  buf.append(dir);
	  buf.append(file);
</COMMAND>
</COMMANDS>
</COMMANDO>
